import sys
from collections import deque

def bfs():
    # 1차원 배열
    # N-1의 빈 공간에 부호를 대입시켜 최대 최소 계산
    # 모든 경우의 수 탐색
    # Queue()
    
    que = deque()
    
    for i in range(n):
        graph
    
    return

N = map(int,sys.stdin.readline())
graph=[0 for i in range(2*N-1)]
graph.append(list(map()))

plus, minus, multi, divid = map(int,sys.stdin.readline().split())

